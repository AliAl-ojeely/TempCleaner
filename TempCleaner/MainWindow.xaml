<Window x:Class="TempCleaner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TempCleaner" Height="680" Width="420"
        WindowStartupLocation="CenterScreen"
        Background="#000000" 
        Foreground="White"
        ResizeMode="CanMinimize">
    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#111111"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="#333333" 
                                BorderThickness="1" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Left" Margin="20,0,0,0" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#222222"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TopIconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1A1A1A"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid x:Name="LayoutRoot">
        <Grid Margin="20">
            <!-- Top bar with language toggle and dev info -->
            <Grid>
                <Button x:Name="LangToggleBtn" Style="{StaticResource TopIconButtonStyle}" 
                        Width="50" Height="30" HorizontalAlignment="Left" VerticalAlignment="Top"
                        Click="LangToggle_Click">EN</Button>

                <Button x:Name="DevInfoButton" Width="30" Height="30" HorizontalAlignment="Right" VerticalAlignment="Top" 
                        Style="{StaticResource TopIconButtonStyle}" Click="DevInfo_Click">
                    <TextBlock Text="!" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button>
            </Grid>

            <StackPanel Margin="0,15,0,0">
                <TextBlock x:Name="MainTitleText" Text="TEMP CLEANER" FontSize="28" FontWeight="Black" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock x:Name="SubtitleText" Text="System Optimization Tool" Foreground="#666666" HorizontalAlignment="Center" Margin="0,0,0,25"/>

                <Button x:Name="CleanTempButton" Style="{StaticResource ModernButtonStyle}" Margin="0,6" Click="CleanTemp_Click">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8B7;" FontSize="20" Margin="0,0,15,0" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TempCleanText" Text="Clean Temp Files" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>

                <Button x:Name="CleanPrefetchButton" Style="{StaticResource ModernButtonStyle}" Margin="0,6" Click="CleanPrefetch_Click">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE81C;" FontSize="20" Margin="0,0,15,0" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                        <TextBlock x:Name="PrefetchCleanText" Text="Clear Prefetch &amp; Recent" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>

                <Button x:Name="CleanRecycleButton" Style="{StaticResource ModernButtonStyle}" Margin="0,6" Click="CleanRecycle_Click">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE74D;" FontSize="20" Margin="0,0,15,0" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                        <TextBlock x:Name="RecycleCleanText" Text="Empty Recycle Bin" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>

                <Button x:Name="FlushDNSButton" Style="{StaticResource ModernButtonStyle}" Margin="0,6" Click="FlushDNS_Click">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE12B;" FontSize="20" Margin="0,0,15,0" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                        <TextBlock x:Name="FlushDNSText" Text="Flush DNS Cache" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>

                <Button x:Name="CleanUpdateButton" Style="{StaticResource ModernButtonStyle}" Margin="0,6" Click="CleanUpdate_Click">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE895;" FontSize="20" Margin="0,0,15,0" Foreground="#AAAAAA" VerticalAlignment="Center"/>
                        <TextBlock x:Name="UpdateCleanText" Text="Clean Win Update Cache" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>

                <Separator Background="#222222" Margin="0,15"/>

                <Button x:Name="RunAllButton" Background="#FFFFFF" Foreground="Black" FontSize="18" FontWeight="Black" Height="60" Cursor="Hand" Click="RunAll_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <ProgressBar x:Name="CleanProgress" Height="2" Background="Transparent" Foreground="#1DB954" BorderThickness="0" Margin="0,15,0,0" IsIndeterminate="True" Visibility="Collapsed"/>

                <TextBlock x:Name="StatusText" Text="Ready" Foreground="#1DB954" HorizontalAlignment="Center" Margin="0,15,0,10" FontWeight="SemiBold"/>

                <CheckBox x:Name="AutoCloseCheckBox" Content="Close app after cleanup" Foreground="#777777" HorizontalAlignment="Center" FontSize="12">
                    <CheckBox.LayoutTransform>
                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                    </CheckBox.LayoutTransform>
                </CheckBox>
            </StackPanel>
        </Grid>

        <!-- Dev modal (unchanged) -->
        <Grid x:Name="DevModal" Background="#E6000000" Visibility="Collapsed" Panel.ZIndex="100">
            <Border Background="#0A0A0A" BorderBrush="#333333" BorderThickness="1" CornerRadius="10" 
                    HorizontalAlignment="Center" VerticalAlignment="Center" Padding="30" Width="300">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="👻" FontSize="45" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                    <TextBlock x:Name="DevModalTitleText" Text="Developer Info" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <TextBlock x:Name="DevNameText" Text="Ali Al-ojeely" Foreground="White" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="DevNicknameText" Text="(Mr.Ghost)" Foreground="#AAAAAA" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock x:Name="DevEmailText" Text="alialojeely@gmail.com" Foreground="#888888" FontSize="13" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock HorizontalAlignment="Center" Margin="0,0,0,15">
                        <Hyperlink NavigateUri="https://github.com/AliAl-ojeely" RequestNavigate="Hyperlink_RequestNavigate" Foreground="#1DB954" TextDecorations="None" FontWeight="Bold">
                            <TextBlock x:Name="GitHubLinkText" Text="GitHub Profile"/>
                        </Hyperlink>
                    </TextBlock>
                    <TextBlock x:Name="VersionText" Text="version 2.5" Foreground="#555555" FontSize="12" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <Button x:Name="CloseModalButton" Content="CLOSE" Width="100" Height="40" Background="#111" Foreground="White" BorderBrush="#333" BorderThickness="1" Cursor="Hand" Click="CloseModal_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>